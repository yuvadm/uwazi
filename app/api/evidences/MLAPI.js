//import {APIURL} from '../config.js';

//let API_URL = APIURL;

export default {
  getSuggestions: (url, data) => {
    //return request.get(API_URL + url, data)
    //.then(doneLoading)
    //.catch(handleError);
  }

  //post: (url, data) => {
  //},

  //delete: (url, data) => {
  //}
}
